// src/lib/metadata/viewport.ts
import type { Viewport } from 'next';

/**
 * Default viewport metadata for Next.js pages
 */
export const viewport: Viewport = {
  // You can expand this object with width, initial-scale, etc.
  themeColor: '#4CA77F',
};
